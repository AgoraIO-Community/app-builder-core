/**
 * EVENT_MODE: boolean
 *   true: EVENT (LIVE)
 *   false: MEETING (RTC)
 */

// const isLiveStreamingEnabled: boolean = !!$config.EVENT_MODE;
const isLiveStreamingEnabled: boolean = true; // for deve purpose
export default isLiveStreamingEnabled;
