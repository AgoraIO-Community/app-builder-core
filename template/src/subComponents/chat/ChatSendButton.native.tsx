import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const ChatSendButton = () => {
  return (
    <View>
      <Text>ChatSendButton.native</Text>
    </View>
  );
};

export default ChatSendButton;

const styles = StyleSheet.create({});
